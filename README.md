# Portfolio Monorepo

Этот репозиторий содержит стартовый каркас персонального сайта-портфолио, собранный по вертикально-модульной архитектуре из `rules/portfolio_architecture_plan_v4.md`.

## Что уже заложено

- монорепозиторий с `apps/`, `packages/`, `modules/`, `backend/`, `tools/`;
- общие типы, контракты, ошибки и дизайн-токены;
- каркас публичного frontend и административной панели на React + TypeScript + SCSS;
- backend-скелет на FastAPI-архитектуре с разделением на API, application, domain и infrastructure;
- контентный формат `portfolio.v1` и стартовые демонстрационные данные;
- инфраструктурные шаблоны для Docker Compose, Nginx и CI.

## Ограничения текущей машины

- в среде доступен `node` и `npm`;
- `pnpm`, `uv` и Python-рантайм в `PATH` пока отсутствуют;
- поэтому lock-файлы и реальный запуск backend не были сгенерированы автоматически в этом проходе.

## Ближайшие шаги

1. Установить frontend-зависимости и проверить сборку `apps/web` и `apps/admin`.
2. Подключить Python 3.13 + `uv` и поднять backend-модуль `projects` как первый эталонный vertical slice.
3. Завести автоматический импорт резюме через `tools/cv-importer/`.
4. Перенести демо-контент в PostgreSQL и Redis, оставив YAML/JSON как экспортируемый формат.

## Лицензия

Проект не является open source и помечен как `UNLICENSED`. Любое использование,
копирование, изменение, публикация, развёртывание или продажа допускаются только
по отдельному явному письменному разрешению Дмитрия Александровича Куренкова.
Условия разрешения для друзей или покупателя определяются отдельно. Полный текст
находится в файлах `LICENSE` и `LICENSE.ru.md`.

## Grafana

Grafana запускается как внутренний Compose-сервис без публичного порта и доступна
по `/grafana/`. Nginx проверяет действующую cookie-сессию админки; без неё
пользователь перенаправляется на `/admin/?returnTo=/grafana/`. Перед production-
запуском обязательно задайте уникальный `GRAFANA_ADMIN_PASSWORD` в `.env`.
Datasource и dashboard аналитики автоматически загружаются из
`infrastructure/grafana/provisioning` и `infrastructure/grafana/dashboards`.

## Карточка общей ссылки

Telegram, VK, почтовые клиенты и другие парсеры получают Open Graph/Twitter Card
метаданные прямо из опубликованного snapshot через Nginx SSI. Заголовок и описание
редактируются в разделе `seo` админки, а изображение выбирается через поле Open
Graph. URL изображения содержит checksum публикации, поэтому после изменения и
публикации создаётся новая версия для социальных кешей. Статический fallback —
`apps/web/public/media/social-preview-v1.png`; favicon по умолчанию использует
безопасную SVG-анимацию.
