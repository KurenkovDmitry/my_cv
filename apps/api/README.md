# Apps API

Каталог зарезервирован под generated-клиенты и дополнительные API-артефакты монорепозитория.

Пока основной backend находится в `backend/`, а этот каталог можно использовать для:

- генерации TypeScript SDK из OpenAPI;
- промежуточных proxy-адаптеров;
- mock server для E2E.

