/**
 * Точки входа для дизайн-токенов.
 *
 * Фактические SCSS-переменные, миксины и анимации лежат рядом и подключаются
 * сборщиком приложений напрямую.
 */
export const designTokenPackageName = "@portfolio/design-tokens";

